<Window x:Class="DICOM_Router.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DICOM_Router"
        xmlns:viewmodels="clr-namespace:DICOM_Router.ViewModels"
        xmlns:views="clr-namespace:DICOM_Router.Views"
        mc:Ignorable="d"
        Title="DICOM Router" Height="600" Width="800" ResizeMode="NoResize" ShowInTaskbar="True" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <!-- Data Templates required for showing views via MVVM -->
        <DataTemplate DataType="{x:Type viewmodels:IncomingViewModel}">
            <views:IncomingView DataContext="{Binding}"/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:OutgoingViewModel}">
            <views:OutgoingView DataContext="{Binding}"/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:RulesViewModel}">
            <views:RulesView DataContext="{Binding}"/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:SettingsViewModel}">
            <views:SettingsView DataContext="{Binding}"/>
        </DataTemplate>
        <!-- Custom Button Generated Code -->
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="Button.Static.Background" Color="#FFDDDDDD"/>
        <SolidColorBrush x:Key="Button.Static.Border" Color="#FF707070"/>
        <SolidColorBrush x:Key="Button.MouseOver.Background" Color="#FFBEE6FD"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border" Color="#FF3C7FB1"/>
        <SolidColorBrush x:Key="Button.Pressed.Background" Color="#FFC4E5F6"/>
        <SolidColorBrush x:Key="Button.Pressed.Border" Color="#FF2C628B"/>
        <SolidColorBrush x:Key="Button.Disabled.Background" Color="#FFF4F4F4"/>
        <SolidColorBrush x:Key="Button.Disabled.Border" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground" Color="#FF838383"/>
        <Style TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Background" Value="{StaticResource Button.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true" CornerRadius="10">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.MouseOver.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.MouseOver.Border}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Pressed.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Pressed.Border}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Disabled.Border}"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="{StaticResource Button.Disabled.Foreground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Custom Button Generated Code -->
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition/>
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>

        <!-- MENU BAR -->
        <DockPanel Grid.Column="1" Grid.Row="0" LastChildFill="True" >
            <StackPanel Orientation="Horizontal" Margin="0,0,30,0 " HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Name="incomingButton" Content="Incoming" Margin="5" Width="100" Background="SteelBlue" Foreground="White" FontWeight="Bold" Visibility="Hidden" Click="incomingButton_Click"/>
                <Button Name="outgoingButton" Content="Outgoing" Margin="5" Width="100" Background="SteelBlue" Foreground="White" FontWeight="Bold" Visibility="Hidden" Click="outgoingButton_Click"/>
                <Button Name="serverButton" Content="Settings" Margin="5" Width="100" Background="SteelBlue" Foreground="White" FontWeight="Bold" Visibility="Hidden" Click="serverButton_Click"/>
                <Button Name="rulesButton" Content="Rules" Margin="5" Width="100" Background="SteelBlue" Foreground="White" FontWeight="Bold" Visibility="Hidden" Click="rulesButton_Click"/>
            </StackPanel>
        </DockPanel>
        <!-- MENU BAR -->
        <!-- Main Area -->
        <ContentControl Grid.Column="1" Grid.Row="1" Content="{Binding}"/> <!-- Entry Point for Views-->
        <StackPanel Name="LoadingPanel" Grid.Column="1" Grid.Row="1" VerticalAlignment="Center">
            <Label Name="loadingLabel" VerticalAlignment="Center" HorizontalAlignment="Center">DICOM Router starting now...</Label>
            <ProgressBar Minimum="0" 
             Maximum="100" 
             Value="25" 
             Name="LoadingProgressBar" 
             Foreground="SteelBlue"            
             Height="20" 
             Width="300"/>
        </StackPanel>
        <!-- Main Area -->
    </Grid>
</Window>
